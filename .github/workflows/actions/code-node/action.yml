name: "Get code and install Node JS"
description: "Downlaod repository code and install Node JS"
runs:
  using: "composite"
    - name: Get code
      uses: actions/checkout@v4
    - name: Setup Node
      uses: actions/setup-node@v4
      with:
        node-version: 20